# AI Policy Forum

## Development

```bash
npm install      # Install dependencies
npm start        # Serve locally at http://localhost:8080 (live reload)
npm run build    # Build to _site/
```

Domain provider: Cloudflare